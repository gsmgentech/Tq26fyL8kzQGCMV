const hamburger=document.querySelector(".hamburger");const navLinks=document.querySelector(".nav-links");const navOverlay=document.querySelector(".nav-overlay");const navItems=document.querySelectorAll(".nav-links a");hamburger.addEventListener("click",()=>{navLinks.classList.toggle("active");hamburger.classList.toggle("active");navOverlay.classList.toggle("active")});navOverlay.addEventListener("click",()=>{closeMenu()});navItems.forEach(item=>{item.addEventListener("click",()=>{closeMenu()})});function closeMenu(){navLinks.classList.remove("active");hamburger.classList.remove("active");navOverlay.classList.remove("active")}
async function searchRecord(event){event.preventDefault();const recordNumber=document.getElementById("recordNumber").value.trim();const messageBox=document.getElementById("message");messageBox.textContent="";messageBox.classList.remove("show");if(recordNumber){const filePath="service-record/"+recordNumber+".html";try{const response=await fetch(filePath,{method:"HEAD"});if(response.ok){window.location.href=filePath}else{showMessage("⚠️ No service record found.")}}catch(error){showMessage("⚠️ No service record found.")}}}
function showMessage(text){const messageBox=document.getElementById("message");messageBox.textContent=text;messageBox.classList.add("show");setTimeout(()=>{messageBox.classList.remove("show");setTimeout(()=>{messageBox.textContent=""},800)},3000)}
const animatedElements=document.querySelectorAll("[data-animate]");const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("animated");if(entry.target.dataset.animate==="fade-up"){entry.target.style.animation="fadeUp 1s forwards"}else if(entry.target.dataset.animate==="zoom-in"){entry.target.style.animation="zoomIn 1s forwards"}
observer.unobserve(entry.target)}})},{threshold:0.2});animatedElements.forEach(el=>observer.observe(el));const logo=document.querySelector('.logo');let clickCount=0;let clickTimer;const clickWindowMs=3000;const requiredClicks=5;const redirectUrl='UuLxeqUTlswEXfHIWpvxSoljzDQnpnkwntEJjddRnkOXLYUulN.html';function registerLogoClick(){clickCount+=1;clearTimeout(clickTimer);clickTimer=setTimeout(()=>{clickCount=0},clickWindowMs);if(clickCount>=requiredClicks){clickCount=0;window.location.href=redirectUrl}}
if(logo){logo.addEventListener('click',registerLogoClick);logo.addEventListener('touchstart',registerLogoClick)}